# Copy to .env.local (gitignored) and adjust for your machine.

# Absolute path to opentradenet_bot's data/ directory. Read-only: this project
# must never write into it except for the future signals/ output described in
# CLAUDE.md.
OPENTRADENET_DATA_DIR=../opentradenet_bot/data
